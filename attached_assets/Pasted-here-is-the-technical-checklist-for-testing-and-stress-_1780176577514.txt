here is the technical checklist for testing and stress-testing this configuration before moving it to mainnet staging.Technical Post-Implementation Checklist1. LEB128 Decoder Edge CasesYour recursive LEB128 decoder loop in server/btc-block-scanner.ts needs to be aggressively resilient against malformed script injection.Testing Vector: Craft a dummy OP_RETURN payload where a Varint specifies an unnaturally large size byte or terminates abruptly without setting the Most Significant Bit ($MSB = 0$) properly.Goal: Ensure your parser catches the buffer overflow error or EOF gracefully, logs it as an invalid protocol sequence, and moves to the next output without crashing the startStakeScanner() polling worker loop.2. PSBT Change Output ValidationWhen constructMarketplacePsbt() selects a buyer's UTXO to fund the transaction, the buyer will almost always have a leftover balance (change).Testing Vector: Verify your bitcoinjs-lib v7 transaction builder calculates the dynamic network miner fee correctly ($Fee = \text{Total Input Sats} - \text{Total Output Sats}$) and explicitly maps an Output 4 for Bitcoin change back to the buyer's Taproot address.Goal: Ensure you don't accidentally leave massive dust balances as implicit miner tips, which burns your users' capital on-chain.3. Asynchronous Settlement Race ConditionsBecause Bitcoin confirmation times fluctuate wildly, a user might close their browser window immediately after calling window.unisat.pushPsbt().Testing Vector: Test an instance where the database registers a pending txid via your tracking endpoint, but the user leaves the site before the transaction receives its first block confirmation.Goal: Ensure your background polling listener (trackSettlement) handles state reconciliation entirely headless, successfully flipping the listing state from active to sold solely based on your Esplora node confirmation webhook.What to Monitor NextKeep an eye on your workspace terminal logs for the next few epochs. You should see clean, structured payloads reflecting: